sudo: required

language: ruby

services:
  - docker

before_install:
        - docker build -t prudhviadapa/kube-repo:v1 .
        - docker ps -a
          #on:
          #branch: dev
script:
      - bundle exec rake test
